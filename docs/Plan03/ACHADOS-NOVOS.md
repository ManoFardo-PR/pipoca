# Achados novos durante a execução do Plan03

Coisas descobertas no meio de um passo que **não** fazem parte do escopo dele. Anotar aqui e
seguir; quem fechar a onda decide se viram subtarefa, follow-up ou nada.

Formato: data · passo em que apareceu · o que é · onde · o que fazer · situação.

| Data | Passo | Achado | Onde | Ação sugerida | Situação |
|---|---|---|---|---|---|
| 2026-08-28 | P1 | `verificar` gravava `itens.<nome>.ok = null` para tsc (sem contagem) e a contagem no lugar do booleano nos demais — o `resumo` mostrava `tsc:✗` com tsc limpo. Spread de `contarChecks` sobrescrevia `ok`. | `scripts/plan03.mjs` (`verificar`) | Corrigido no próprio P1: `{ ok, passou, falhou }`. | resolvido (P1) |
| 2026-08-28 | P1 | `run-admin.mjs` flaky: 24/25 numa rodada do `verificar --e2e` (logo após `linha-verde`), 25/25 em 5 rodadas isoladas e na 1ª rodada do `verificar`. Check não identificado porque o monitor engolia a saída. | `tests/e2e/run-admin.mjs` | Monitor agora imprime as linhas de falha (feito no P1). Se repetir, capturar o check e tratar em D7 (harness e2e compartilhado). | encerrado por investigação ativa (pós-F, 2026-09-04): 4 ciclos dirigidos linha-verde→admin = 25/25 nos quatro; nunca reproduziu desde 28/08 (dezenas de rodadas locais + CI), e o D7 refez o harness (bootServer com espera de porta — a causa provável). Reabrir SÓ se reaparecer, anexando a linha do check. |
| 2026-08-28 | A3 | Advisor security também aponta `tenant_da_sessao()` executável por `authenticated` (SECURITY DEFINER). `rls_supabase.sql:245` só revogou `public, anon`; a função é chamada apenas pelo trigger `fixar_tenant_perfil` (SECURITY DEFINER, roda como dono) — `authenticated` provavelmente não precisa. `eh_operador()` e `tenant_vinculado_a_mim()` PRECISAM (políticas RLS). | Supabase `bamlljvllcxdnsheatqv` | Confirmar com um SELECT como authenticated após A5 e, se ninguém quebrar, `revoke execute ... from authenticated` numa migração pequena (follow-up, fora do escopo de A3). | migração escrita (pós-F · 71bd9eb: 2026-09-04_revoke-tenant-da-sessao-authenticated.sql, com pré-checagem); aplicação via prompt-replit-pos-plan.txt |
| 2026-08-28 | A4 | Produção (`config_ia`): `plataforma` e `ten_aced3c71` têm `fallback: deepseek` e `plataforma:global` tem `modeloPadrao` todo `null`. Hoje o fallback funciona pelo default escondido `deepseek-chat` da edge; após o redeploy de A5 (fail-closed) o fallback DeepSeek conta como não configurado até o operador definir o padrão global em SA_IA_GLOBAL. | Supabase `config_ia` · SA_IA_GLOBAL | **A5, antes do redeploy:** dono decide — definir `modeloPadrao.deepseek = deepseek-chat` (e opcionalmente `gemini = gemini-2.5-flash`) no admin, ou aceitar perder o fallback. | resolvido (A5, 2026-08-28): dono mandou incluir modelo legítimo — `plataforma:global.modeloPadrao` = {deepseek: deepseek-chat, gemini: gemini-2.5-flash} via SQL |
| 2026-08-28 | A4 | A linha legada `config_ia.plataforma` usa `modelo: "gemini-flash-latest"`, que não está no catálogo do admin (`MODELOS_POR_PROVEDOR.gemini = ["gemini-2.5-flash"]`); o admin rejeitaria essa config se fosse salva por ele. A edge a envia como está (funciona enquanto o alias existir no Google). | Supabase `config_ia` (`plataforma`) | Trocar para `gemini-2.5-flash` via admin (SA_AI, tenant plataforma) ou SQL de 1 linha em A5; registrar no catálogo. | resolvido (A5, 2026-08-28): `plataforma.modelo` = gemini-2.5-flash via SQL |
| 2026-08-28 | B1 | Pior que UI-C01: `Shell.dc.html` declarava `refRoot` como campo de classe, mas o template só liga `{{ refRoot }}` pelo `renderVals` — `_rootEl` era `undefined` e `_aplicarA11y` nunca aplicou `pip-contrast`/`pip-reduce-motion`/`pip-dyslexia`. Os 3 toggles do PainelA11y eram inertes (o `!important` de `index.html` nunca chegou a atuar). Sonda: `document.querySelector('.pip-contrast')` = null após `setState({a11y:{contrast:true}})`. | `src/telas/Shell.dc.html` | Corrigido no B1 (`refRoot: this.refRoot` no `renderVals`) e coberto por `tests/e2e/sondar-a11y.mjs`. Lição para o guia (PS-04/D5): refs só funcionam devolvidos pelo `renderVals`. | resolvido (B1) |
| 2026-08-28 | A3 | `src/backend/adaptadores/rls_supabase.sql` é "espelho" mas está defasado desde `20260826155239` (políticas ainda na forma `auth.uid()` sem init-plan, sem FKs de cascade, sem `registrar_uso_ia`) — e ficará mais após A5 (políticas fundidas). | `src/backend/adaptadores/rls_supabase.sql` | Decidir em D ou E: virar dump gerado (`pg_dump --schema-only`) ou nota no topo apontando `migrations/` como fonte de verdade. | resolvido (pós-F, 2026-09-04 · 71bd9eb): aviso de HISTÓRICO no topo; fonte de verdade = src/backend/migrations/ |
| 2026-09-01 | C12 | Sonda de alvos do fechamento C: as telas do APP (criança 48+, cuidador 44+) ficaram limpas após os ajustes do C12, mas as telas do ADMIN seguem com alvos abaixo de 44px (tabs 34, switches 28, botões 30–43 em adm2–adm7) — herança de B10/B11, que não fixou piso de alvo. | `src/admin/telas/*` · `docs/auditorias/screenshots/depois-C/sondas.json` | Aplicar piso 44px nas telas do admin (tabs do AdminShell, switches da Seguranca, botões de provedor/modelo) — candidata a follow-up na Onda D (D5) ou passo próprio; o admin é ferramenta de desktop do operador, prioridade menor que o app. | resolvido (pós-F, 2026-09-04 · ebc99fa): min-height 44 global no admin.html + inline min-heights elevados; sonda 81→0 alvos <44px |
| 2026-09-04 | F2 | UI-C58 fechou só pela metade: a T7 empilha no retrato (`@media ≤700px`), mas a T4 (Tela4Heroi) segue sem media query própria — sobrevive por flex, sem plano explícito para 390px. | `src/telas/Tela4Heroi.dc.html` | `@media ≤700px` no padrão da T3/T7 (palco acima, banco abaixo, alvos mantidos ≥48). Polimento; nenhum bloqueio observado nos screenshots. | resolvido (pós-F, 2026-09-04 · ebc99fa): @media ≤700px — palco rola, CTA empilha |
| 2026-09-04 | F2 | UI-A12/A33/A35/A36 parciais: tokens do B2 corrigidos e admin no sistema (B3), mas seguem hex crus em telas adultas/admin, a 3ª terracota `#b8693c` e a landing autocontida com `var(--pip-*, fallback)` que não resolvem (3 fontes de verdade p/ 1 marca). | `src/admin/telas/*`, `landing.html`, `src/telas/PainelCuidador.dc.html` | Tokenização incremental (trocar hex por var ao tocar cada arquivo) + decidir se a landing importa `tokens.css`. Polimento visual, sem risco funcional. | núcleo resolvido (pós-F, 2026-09-04 · ebc99fa): --pip-terracota-marca criado; landing já importa tokens.css e usa os tokens; contador do PainelCuidador corrigido. Hex crus restantes: regra incremental (trocar ao tocar cada arquivo) |

# faseFF · FF-NN · &lt;Título do sub-passo&gt;

> Gabarito. Copie e preencha **todas** as seções abaixo, na mesma ordem. O [[check_plans]] valida a
> presença das seções, os `[[links]]` e os nomes canônicos. Idioma: PT-BR.

## Identidade
- id: `faseFF-FF-NN`
- nó(s) da arquitetura: &lt;ex.: T4 — apenas os nós que ESTE doc OWNa; consumo vai em "Relações"&gt;
- tela(s) do brief: &lt;1..8 ou "—"&gt;
- classe: &lt;mvp | f2 | pivot | admin | —&gt;

## Objetivo
Uma frase: o que este sub-passo entrega.

## Pré-requisitos / Depende de
Lista de ids `[[faseFF-FF-NN]]` de fases anteriores ou iguais (sem dependência para frente, sem ciclo).

## Arquivos afetados
Caminhos exatos a criar/editar/remover (ex.: `src/telas/Tela4Heroi.dc.html`, `src/core/historia.ts`).

## Nomes & variáveis
Todo identificador novo ou reaproveitado (estado, props, refs, handlers), colado ao vocabulário de
[[_contratos/tipos-core]] / [[_contratos/eventos-acoes]]. Diga de onde veio no protótipo quando aplicável.

## Interfaces / contratos
Os tipos / JSON-schemas tocados, **pelo nome canônico** ([[_contratos/tipos-core]], [[_contratos/schemas-json]]).

## Regras de negócio
Regras concretas e testáveis (ex.: 2/3·1/3; regra_de_ouro; nunca X vermelho; idempotência do award;
degradação aberto→convergente).

## Passos de implementação
Lista ordenada e acionável.

## Estados / edge-cases
Vazio · lendo · sucesso · re-tentativa acolhedora · sem voz pt-BR · IA refusal→Motor A · PIN errado · etc.

## Critérios de aceitação / verificação
Checklist testável (inclua as fixtures de [[fase00-00-21]] quando o motor estiver envolvido).

## Relações com outros docs
- Depende de: `[[...]]`
- É consumido por: `[[...]]`
- Reconcilia / conserta: `[[...]]` (ex.: `[[fase00-00-20]]` para tudo que toca a tira)

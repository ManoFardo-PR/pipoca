# faseFF · FF-NN · <Título do sub-passo>

> Gabarito do plans02. Copie e preencha **todas** as seções abaixo, na mesma ordem. O [[check_plans02]]
> valida a presença e a ordem das seções, os `[[links]]` e os nomes canônicos. Idioma: PT-BR.
> **Escopo dos links:** `[[links]]` resolvem SÓ dentro do plans02 (docs `faseNN-NN-NN` das fases 10–14
> e arquivos da raiz do plans02). Contratos do plans02 serão referenciáveis quando existirem; contratos
> legados da geração 1 são citados por caminho simples, sem `[[...]]` (ex.: `docs/plans/_contratos/tipos-core.md`).

## Identidade
- id: `faseFF-FF-NN`
- nó(s) da arquitetura: <nós que ESTE doc OWNa, ou "—">
- tela(s) do brief: <1..8 ou "—">
- classe: <mvp | f2 | pivot | admin | —>

## Objetivo
Uma frase: o que este sub-passo entrega.

## Pré-requisitos / Depende de
Lista de ids `[[faseFF-FF-NN]]` de fases anteriores ou iguais (sem dependência para frente, sem ciclo).

## Arquivos afetados
Caminhos exatos a criar/editar/remover (ex.: `docs/fichas/objetos.v1.json`, `src/core/fichas/lint_fichas.ts`).

## Nomes & variáveis
Todo identificador novo ou reaproveitado (tipos, campos, arquivos, handlers), colado ao vocabulário dos
contratos do plans02 quando existirem; contratos legados citados por caminho simples
(ex.: `docs/plans/_contratos/tipos-core.md`). Diga de onde veio quando aplicável.

## Interfaces / contratos
Os tipos / JSON-schemas tocados, **pelo nome canônico** (contratos do plans02 quando existirem;
legados por caminho simples, ex.: `docs/plans/_contratos/schemas-json.md`).

## Regras de negócio
Regras concretas e testáveis (invariantes, limites, políticas de falha).

## Passos de implementação
Lista ordenada e acionável.

## Estados / edge-cases
Vazio · lendo · sucesso · falha do provedor · dado ausente · etc.

## Critérios de aceitação / verificação
Checklist testável.

## Relações com outros docs
- Depende de: `[[...]]`
- É consumido por: `[[...]]`
- Reconcilia / conserta: `[[...]]`

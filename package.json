{
  "name": "pipoca",
  "version": "0.1.0",
  "description": "Pipoca — app de leitura guiada para crianças",
  "scripts": {
    "build": "cd dc-runtime && bun run build",
    "serve": "node server.js",
    "typecheck": "bun x tsc --noEmit",
    "test": "bun run src/motores/motor.test.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
